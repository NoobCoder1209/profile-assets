name: profile

# Daily auto-regen of dynamic README assets used by NoobCoder1209/NoobCoder1209.
# Snake animation goes to the `output` branch of THIS repo.
# Metrics SVGs go to `assets/` on `main` of THIS repo.
# The profile README references all of them via raw.githubusercontent.com.

on:
  schedule:
    - cron: "37 4 * * *"
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - .github/workflows/profile.yml

permissions:
  contents: write

jobs:
  snake:
    name: Generate contribution snake
    runs-on: ubuntu-latest
    steps:
      - name: Generate snake SVGs
        uses: Platane/snk@v3
        with:
          github_user_name: NoobCoder1209
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      - name: Push to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  metrics-overall:
    name: Generate overall metrics SVG
    runs-on: ubuntu-latest
    steps:
      - name: Light theme
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN || secrets.GITHUB_TOKEN }}
          user: NoobCoder1209
          filename: assets/metrics.svg
          template: classic
          base: header, activity, community, repositories, metadata
          config_timezone: Europe/Sofia
          plugin_lines: yes
          plugin_followup: yes
          plugin_topics: yes
          plugin_topics_mode: icons
          plugin_topics_limit: 12
          committer_branch: main
          committer_message: "chore: refresh metrics.svg"

      - name: Dark theme
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN || secrets.GITHUB_TOKEN }}
          user: NoobCoder1209
          filename: assets/metrics-dark.svg
          template: classic
          base: header, activity, community, repositories, metadata
          config_timezone: Europe/Sofia
          plugin_lines: yes
          plugin_followup: yes
          plugin_topics: yes
          plugin_topics_mode: icons
          plugin_topics_limit: 12
          committer_branch: main
          committer_message: "chore: refresh metrics-dark.svg"

  metrics-languages:
    name: Generate languages SVG
    runs-on: ubuntu-latest
    needs: metrics-overall
    steps:
      - name: Light theme
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN || secrets.GITHUB_TOKEN }}
          user: NoobCoder1209
          filename: assets/metrics-languages.svg
          template: classic
          base: ""
          config_timezone: Europe/Sofia
          plugin_languages: yes
          plugin_languages_ignored: html, css, scss, less
          plugin_languages_details: bytes-size, percentage
          plugin_languages_limit: 8
          plugin_languages_sections: most-used
          committer_branch: main
          committer_message: "chore: refresh metrics-languages.svg"
